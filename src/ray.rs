#[cfg(test)]
mod ray_tests {
    use crate::elementary::tuple::Tuple;
    use crate::elementary::float::Float;

    #[test]
    fn a_tuple_with_w_1_is_a_point() {
        let result = 2 + 2;
        assert_eq!(result, 4);
        // let tuple = Tuple::new(4.3, -4.2, 1.0, 1.0);
        // let a = Float::new(1.0);
    }
}
